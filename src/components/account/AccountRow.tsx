import React from 'react'

interface Props {
    src: string | undefined;
    userName: string;
}

export const AccountRow: React.FC<Props> = (props: Props) => {

    return (
        <div>
            <img src=""></img>
            <h3></h3>
        </div>
    )
}
